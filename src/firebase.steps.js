/**
 * create a firebase project
 * build > authentication > goggle sign in enable
 * project settings > web application
 * firbase install
 * config copy
 * in the config: initialize the auth of firebase application
 */